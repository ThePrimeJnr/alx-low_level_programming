#include "main.h"

int _square_div(int i)
{
	
}




int _sqrt_recursion(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		j = _square_div(i +1)
		return (_sqrt_recursion(n / (j * j));
	}
}
