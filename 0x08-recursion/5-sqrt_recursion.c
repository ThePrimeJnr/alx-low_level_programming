#include "main.h"

int _square_div(int i)
{
	if (n

int _sqrt_recursion(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (_square_div(i + 1) + _sqrt_recursion);
	}
}
